# AGENTS.md — Workspace Rules

Refer to root [AGENTS.md](../AGENTS.md) for full governance.

## Mandatory Workspace Directives
1. **Peer Review**: Run `peer-evaluator` subagent before advancing any completed work node.
2. **Context7**: Use `context7` MCP tools (`resolve-library-id` + `query-docs`) for external library contracts.
3. **Graphify First**: Query `codebase-memory-mcp` before scanning or reading raw workspace files.
4. **Convex Guidelines**: Consult `convex/_generated/ai/guidelines.md` for Convex backend code.
