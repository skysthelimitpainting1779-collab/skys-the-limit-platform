# Workflow: autoloop

Follow the autoloop skill installed at `.agents/skills/autoloop/SKILL.md` to discover tasks, issues, bugs, and improvements, compile them into `.agent/graph/foundation.graph.json`, and run the closed-loop execution engine automatically.
