# GENERATED by npm run host:compile

prefix_rule(
    pattern = ["git","add","."],
    decision = "forbidden",
    justification = "Stage explicit files instead of git add .",
    match = ["git add ."],
)

prefix_rule(
    pattern = ["git","add","-A"],
    decision = "forbidden",
    justification = "Stage explicit files instead of git add -A",
    match = ["git add -A"],
)

prefix_rule(
    pattern = ["git","commit","-a"],
    decision = "forbidden",
    justification = "Stage explicit files before committing",
    match = ["git commit -a"],
)

prefix_rule(
    pattern = ["git","reset","--hard"],
    decision = "forbidden",
    justification = "Use a scoped, recoverable change instead of hard reset",
    match = ["git reset --hard"],
)

prefix_rule(
    pattern = ["git","clean","-fd"],
    decision = "forbidden",
    justification = "Do not destructively clean the worktree",
    match = ["git clean -fd"],
)

prefix_rule(
    pattern = ["git","push","--force"],
    decision = "forbidden",
    justification = "Force-push is prohibited",
    match = ["git push --force"],
)

prefix_rule(
    pattern = ["git","push","-f"],
    decision = "forbidden",
    justification = "Force-push is prohibited",
    match = ["git push -f"],
)
